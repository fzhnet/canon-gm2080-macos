# Local machine configuration — contains your printer's address.
bridge/docker/.env

# Native driver build output.
native/build/
native/dist/
native/rastertocanonijgm

# macOS Finder metadata.
.DS_Store
